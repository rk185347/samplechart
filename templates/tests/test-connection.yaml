apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rkchart.fullname" . }}-test-connection"
  labels:
    {{- include "rkchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rkchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
