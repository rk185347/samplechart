apiVersion: v1
kind: Service
metadata:
  name: {{ include "rkchart.fullname" . }}
  labels:
    {{- include "rkchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rkchart.selectorLabels" . | nindent 4 }}
